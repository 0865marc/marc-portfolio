name: Actions diagnostic

'on':
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  diagnostic:
    name: Diagnostic
    runs-on: ubuntu-latest
    steps:
      - run: echo 'GitHub Actions workflow parsing is available.'
